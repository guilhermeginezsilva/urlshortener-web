@import "styles";

:host {
    height: 100%;
}

section {
    height: 100%;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
}

main {
    flex-grow: 1;
    height: calc(100% - #{$footerHeight} - #{$headerHeight});
}